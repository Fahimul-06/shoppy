import React, { useEffect, useState } from 'react';
import { api, getToken } from '../../lib/api';
export default function AdminOrdersTab(){ const [orders,setOrders]=useState<any[]>([]); const load=async()=>{const r=await api.get<{orders:any[]}>('/admin/orders',getToken('admin')); setOrders(r.orders)}; useEffect(()=>{load()},[]); const update=async(id:string,field:string,value:string)=>{await api.patch(`/admin/orders/${id}`,{[field]:value},getToken('admin')); await load()}; return <div className="bg-white rounded-2xl border overflow-x-auto"><h2 className="text-xl font-black p-4">Orders</h2><table className="w-full text-sm"><thead className="bg-gray-50"><tr><th className="p-3 text-left">Order</th><th>Customer</th><th>Total</th><th>Status</th><th>Payment</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} className="border-t"><td className="p-3"><b>{o.orderNumber}</b><p className="text-xs text-gray-500">{new Date(o.createdAt).toLocaleString()}</p></td><td className="text-center">{o.user?.email || 'Customer'}</td><td className="text-center">৳{Number(o.totalAmount).toLocaleString()}</td><td className="text-center"><select value={o.status} onChange={e=>update(o.id,'status',e.target.value)} className="border rounded-lg px-2 py-1"><option>pending</option><option>processing</option><option>shipped</option><option>delivered</option><option>cancelled</option></select></td><td className="text-center"><select value={o.paymentStatus} onChange={e=>update(o.id,'paymentStatus',e.target.value)} className="border rounded-lg px-2 py-1"><option>pending</option><option>paid</option><option>failed</option><option>refunded</option></select></td></tr>)}</tbody></table></div>}
