import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowLeft, Loader2, Lock, Mail, Store } from 'lucide-react';
import { api, setSession } from '../../lib/api';
import { SELLER_DASHBOARD_PATH, SELLER_REGISTER_PATH } from '../../lib/adminPortal';
export default function SellerLoginPage(){ const nav=useNavigate(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); const login=async()=>{setLoading(true);setError('');try{const r=await api.post<{token:string;seller:any}>('/seller/login',{email,password}); setSession('seller',r.token,r.seller); nav(SELLER_DASHBOARD_PATH)}catch(e){setError(e instanceof Error?e.message:'Login failed')}finally{setLoading(false)}}; return <div className="min-h-screen bg-orange-50 flex items-center justify-center p-4"><div className="bg-white rounded-3xl p-7 w-full max-w-md border"><div className="w-14 h-14 bg-orange-500 rounded-2xl flex items-center justify-center mb-4"><Store className="text-white"/></div><h1 className="text-2xl font-black">Seller Login</h1><p className="text-sm text-gray-500 mb-5">Manage your products and shop.</p><div className="space-y-3"><div className="relative"><Mail size={16} className="absolute left-3 top-3.5 text-gray-400"/><input className="w-full pl-10 border rounded-xl p-3 text-sm" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/></div><div className="relative"><Lock size={16} className="absolute left-3 top-3.5 text-gray-400"/><input className="w-full pl-10 border rounded-xl p-3 text-sm" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/></div>{error&&<p className="text-sm text-red-500 bg-red-50 p-3 rounded-xl">{error}</p>}<button onClick={login} disabled={loading} className="w-full bg-orange-500 text-white font-bold py-3 rounded-xl flex justify-center gap-2">{loading&&<Loader2 className="animate-spin" size={16}/>} Login</button><Link to={SELLER_REGISTER_PATH} className="block text-center text-sm text-orange-600 font-semibold">Create seller account</Link><Link to="/" className="text-sm text-gray-400 flex justify-center gap-1"><ArrowLeft size={14}/> Back to store</Link></div></div></div> }
