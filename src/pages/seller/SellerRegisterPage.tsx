import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowLeft, Loader2, Store } from 'lucide-react';
import { api } from '../../lib/api';
export default function SellerRegisterPage(){ const nav=useNavigate(); const [form,setForm]=useState({name:'',email:'',phone:'',password:'',shopName:'',shopAddress:'',businessType:'',nidNumber:'',tinNumber:'',bankName:'',bankAccount:''}); const [loading,setLoading]=useState(false); const [msg,setMsg]=useState(''); const submit=async()=>{setLoading(true);setMsg('');try{await api.post('/seller/register',form); setMsg('Registration submitted. Admin approval is required before selling.'); setTimeout(()=>nav('/seller/login'),1200)}catch(e){setMsg(e instanceof Error?e.message:'Registration failed')}finally{setLoading(false)}}; const fields=[['name','Owner name'],['email','Email'],['phone','Phone'],['password','Password'],['shopName','Shop name'],['shopAddress','Shop address'],['businessType','Business type'],['nidNumber','NID number'],['tinNumber','TIN number'],['bankName','Bank name'],['bankAccount','Bank account']]; return <div className="min-h-screen bg-gray-50 p-4"><div className="max-w-2xl mx-auto bg-white rounded-3xl p-7 border"><div className="flex items-center gap-3 mb-4"><div className="w-12 h-12 bg-orange-500 rounded-2xl flex items-center justify-center"><Store className="text-white"/></div><div><h1 className="text-2xl font-black">Seller Registration</h1><p className="text-sm text-gray-500">Create seller profile for admin review.</p></div></div><div className="grid sm:grid-cols-2 gap-3">{fields.map(([k,l])=><input key={k} type={k==='password'?'password':'text'} className="border rounded-xl p-3 text-sm" placeholder={l} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}</div>{msg&&<p className={`text-sm mt-4 p-3 rounded-xl ${msg.startsWith('Registration')?'bg-green-50 text-green-700':'bg-red-50 text-red-500'}`}>{msg}</p>}<button onClick={submit} disabled={loading} className="mt-4 w-full bg-orange-500 text-white font-bold py-3 rounded-xl flex justify-center gap-2">{loading&&<Loader2 className="animate-spin" size={16}/>} Submit Registration</button><div className="mt-4 flex justify-between text-sm"><Link to="/seller/login" className="text-orange-600 font-semibold">Already registered?</Link><Link to="/" className="text-gray-400 flex gap-1"><ArrowLeft size={14}/> Store</Link></div></div></div>}
